#pragma once
#include "List.h"

class Operations
{
public:
    int sum(List *stack);
    int sub(List *stack);
    int mul(List *stack);
    int div(List *stack);
    int ifing(List *stack);
    int min(List *stack);
};